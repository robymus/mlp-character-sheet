import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    base: './',

    // You can add plugins or server settings here later
    server: {
        port: 5173,
    }
})